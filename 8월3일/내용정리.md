@붙이는거 유심히 보기 (컴퓨터가 보라는 주석), @를 보고 Spring이 자동으로 동작을 만들어줌, 이 코드를 어떻게 다뤄야 하는지 알려주는 지시표

    이 @가 어디에 붙었는가?
    Spring에게 무슨 역할을 알려주는가?
    이 @ 때문에 Spring이 자동으로 무엇을 해주는가?

    @붙음
    ↓
    Spring이 읽음
    ↓
    해당 클래스/메서드의 역할을 파악
    ↓
    필요한 객체 생성·연결·HTTP 처리 등을 자동 수행

그리들 왜하느냐? : 파일을 역할과 책임에 따라 분할해서 실행하게됨. 그리들을 걸고 디펜던시 걸어줌, 소스코드를 실행할수 있도록 묶어 정리

노트 작성할때 어떤 사용자한테 어떤 역할과 기능이 필요할지도 정리합시다, 아키텍쳐 관점에서의 이해

solid원칙(결국 역할과 책임을 구분하기 위함)

    S : 단일 책임 원칙 : 하나의 클래스는 하나의 책임만 가능 : controller, servic, repository등 계층 분리
    O : 개방-폐쇄 원칙 : 기존 코드는 수정하지 않고 기능을 확장할 수 있어야한다 : 인터페이스 기반 구현체 추가, 전략 패턴 활용
    L : 리스코프 치환 원칙 : 부모 타입 대신 자식 객체를 사용해도 동일하게 동작해야한다 : 인터페이스 구현체 교체 가능
    I : 인터페이스 분리 원칙 : 필요한 기능만 가진 작은 인터페이스로 분리한다. : 역할별 인터페이스 설계(작고 구체적인 인터페이스 여러개)
    D : 의존성 역전 원칙 : 구현 클래스가 아닌 추상화(인터페이스)에 의존한다 : soring의 DI 와 IoC의 핵심 원칙(객체 연결을 인터페이스로)

객체 생성 패턴 : 왜 패턴이 필요한가: 코드 중복, 의존성 증가, 테스트 어려움

    싱글톤 : 객체를 하나만 생성 : 매번 new선언을 해서 객체 생성해야됨
    팩토리 패턴 : new를 쓰지않고, 무엇을 만들지를 공장(factor)에 맡기는 방식 : 결합도 감소, 확장 용이, 다형성 활용 : 인터페이스, factory클래스+if else문
    빌더 패턴 : 필드가 많거나 선택적 값이 많은 객체를 안전하게 생성하기 위한 패턴 : 매개변수 순서 안헷갈리게

라이브러릐 의존성 추가

왜 나누는가? 객체 지향 srp, ocp/dip : 클래스는 단 하나의 책임만.
스프링부트 : di컨테이너 , auto configuration , @serive/@repository
Rest API : @restcontroller, HTTP Method/staus, postman

절차기반(함수중심 - 데이터와 로직이 분리, 수정시 전체 흐름에 영향), 객체 지향(데이터와 동작이 묶임, 책임분리)

스프링 부트 : 자동설정, di컨테이너 = 나눠진 객체들을 자동으로 연결해주는 도구(설정 자동화, 톰캣, 의존성, @)
분리 후 수동 연결 코드가 많아짐 -> 스프링이 자동으로 객체 생성해줌(main()에서 new로 수동으로 생성/주입하던걸 @service/@autowired 로 주입)

빈(클래스를 가지고 만든 객체(목적성을 가지고있음)), 엔터프라이즈 자바 빈즈(주로 사용되는(비슷한) 클래스들 정의)
-> IoC컨테이너(객체들을 관리하는 하나의 컨테이너 박스), 직접 객체를 생성하는 경우는 잘 없음(객체를 생성하는 부분이 없음), 스캐너가 모든 클래스 파일 탐색(@붙은거 찾음->객체 자동 생성->IoC컨테이너 담음, 설정할@에 따라 객체의 역할들이 부여됨, IoC컨테이너 안에 클래스들은 언제든지,어디든 마음대로 꺼내 쓸 수 있음, 객체들간의 값을 주고받는데에 쉬워짐, @은 언제 객체를 생성되는지, 역할은 무엇인지, 얘가 가지고 있는 메서드는 언제 호출되는지 정도만 꼭 파악, 지금은 동작 원리 이해할려고 하지마)

객체가 다른 구체 객체를 직접 만들고 의존하면 독립성이 깨질 수 있다. 그래서 Spring은 객체 생성·연결을 IoC Container에 넘기고, 인터페이스를 사이에 두는 DI 방식으로 결합도를 낮춘다.

java/loginauth
-config : 설정 클래스 : 애플리케이션이 어떤 설정으로 동작하는지(db연결, 포트 등)설정, application.yml과 config패키지를 함꼐 다룸
-domain : 도메인 객체 : 데이터의 형태 : @Entity
-exception : 예외처리
-repository(dao) : 실제 DB접근 계층
-security : 보안(인증/인가를 어떻게 처리하는지)
-service : 비즈니스 로직 계층(인증 처리 등)
-web(controller) : HTTP 요청을 받는 계층 : 창구
-LoginAuthApplication.java : main()이 있는 프로그램 시작점, 톰캣을 띄우며 실행 : @SpringBootApplication
-resource/static : html 등 실제 사용자가 보는 화면, 컨트롤러와 연결

# Spring Boot 프로젝트 구조를 "실행 흐름"을 따라가는 순서로 이해합니다.

1. 진입점(Application.java) : LoginAuthApplication.java부터 시작합니다. Spring Boot는 이 파일의 main() 메서드에서 내장 서버(Tomcat)를 띄우며 실행되므로, 프로젝트의 시작점이 됩니다.

2. 설정(config, application.yml) : 애플리케이션이 어떤 설정으로 동작하는지(DB 연결, 포트 등) 설명합니다. application.yml과 config 패키지를 함께 다룹니다.

3. 요청 진입 → 도메인 → 저장소 순서 (web → service → domain/repository)

- web: 사용자의 HTTP 요청을 받는 컨트롤러 계층 (예: 로그인 요청을 받는 곳)
- service: 실제 비즈니스 로직(인증 처리 등)
- domain: 데이터의 형태(User 엔티티 등)
- repository: DB와 실제로 상호작용하는 계층
  즉, "요청이 들어와서 → 처리되고 → 저장되는" 흐름 순서로 이해할 수 있습니다.

4. 보안(security) : 인증/인가를 어떻게 처리하는지(Spring Security 설정) 제시합니다. 로그인 인증 프로그램이니 이 부분이 핵심 포인트가 될 것입니다.

5. 예외 처리(exception) : 에러 상황을 어떻게 핸들링하는지 제시합니다.

6. 화면(resources/static) : login.html, signup.html, home.html 등 실제 사용자가 보는 화면과, 이 화면들이 앞서 설명한 컨트롤러와 어떻게 연결되는지 이해합니다.

# Spring Boot는 Annotation이 핵심입니다. Annotation은 기계가 읽는 주석이라는 뜻이며, 실제로 일을 하는 건 그 태그를 읽고 반응하는 스프링의 엔진(컴포넌트 스캐너, 리플렉션, 빈 팩토리)입니다.

# 궁금한점

// 실제 연결을 생성하는 메서드, JDBC를 사용하여 MariaDB에 연결
//Connection은 우리가 직접 만든 클래스가 아니라 JDBC의 인터페이스
//Connection 객체를 얻으면 Java에서 그 DB에 SQL 명령을 보내고 결과를 받아서 다룰 수 있다.
private Connection getConnection() throws SQLException {
return DriverManager.getConnection(url, user, password);
}

톰캣

빌더 패턴 추가 공부

D : 의존성 역전 원칙

@Value — application.yml의 값을 필드에 하나씩 꺼내 주입합니다.

@Repository — DB 접근 담당 빈임을 표시하고, DB 예외를 스프링이 이해하는 예외로 바꿔줍니다.

의존성 주입
@Autowired — 필요한 빈을 스프링이 찾아서 자동으로 연결(주입)해 줍니다.
@RequiredArgsConstructor(Lombok) — final 필드 기준으로 생성자를 자동 생성해서, 생성자 주입을 간편하게 해줍니다.

리팩토링 : 기능은 변화없는데 구조가 변화는거

스프링부트가 객체간 연결을 정해진게 있어서 로직대로 해준다면, 각 객체별로도 정해진 틀(매개변수라던가 타입설정 같은거..)이 있나?

DI한다?

@마다 객체 생성하는것 뿐만 아니라 추가 기능도 달려있는 경우가 있다. 어떠한 역할을하는@인지 파악
controller? : 분기의 역할. 흐름을 따로 설정.
service? : 객체만 자동생성
repository? : 객체만 자동생성

웹서버와 웹브라우저간 연결에서, 웹서버는 웹브라우저가 필요한 정보만 보내자 -> json, 뷰
Rest API : 웹서버는 필요한 정보만 보내고 다른 무거운 작업들 사용자의 웹브라우저에서 실행되게 하자, 서버와 클라이언트간에는 데이터만 주고받음, 서버 부담 줄어듬
