import pandas as pd
import matplotlib.pyplot as plt

# 서울시 상관 추정 매출을 흉내 낸 작은 표
df = pd.DataFrame({
    "서비스_업종": ["한식", "카페", "한식", "편의점", "카페", "미용실"],
    "당월_매출": [820, 310, 640, 450, 280, 190],       # 단위: 만원
    "연령대_10": [20, 60, 15, 90, 70, 10],             # 10대 매출
    "연령대_20": [180, 140, 150, 160, 120, 60],        # 20대 매출
    "연령대_30": [260, 80, 210, 120, 70, 80],          # 30대 매출
})

# 업종별로 묶어 합계 -> 내림차순 정렬 -> 상위 3개만
top = df.groupby("서비스_업종")["당월_매출"].sum().sort_values(ascending=False).head(3)
print(top)

# 연령대 3개의 열의 열별 합계로 막대 그래프를 그려 파일로 저장
ages = df[["연령대_10","연령대_20","연령대_30"]].sum()
ages.plot(kind = 'bar', title = 'age sales') # 합계 3개를 막대 그래프로
plt.tight_layout() # 글자가 잘리지 않게 여백 자동 조정
plt.savefig('age_sales.png') # 화면 표시 대신 PNG 파일로 저장
print('age_sales.png 저장완료') # 보고서에 바로 붙을 파일 이미지 완성

"""
궁금했던점

df["연령대_10", "연령대_20", "연령대_30"] 는 어떤 의미?
"연령대_10", "연령대_20", "연령대_30" 세 열을 선택하는 게 아니라, 저 세 문자열이 묶인 하나의 튜플 이름을 가진 열을 찾으려 함.

plt 는 현재 활성화 된 그래프를 설정함, 그렇다면 그래프를 여러개 활성화 해버리면?
마지막 활성화 그래프 기준,

fig, ax를 각각 변수로 받는 방식 추천

    ax1 = ages.plot(kind="bar", title="연령대별 매출")
    fig1 = ax1.get_figure(
"""