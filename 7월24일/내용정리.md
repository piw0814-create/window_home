반응형 css

    맨끝에 @~

자바스크립트

    var let const
    스코프(변수에 접근할 수 있는 범위)와 호이스팅

    변수 암것도 없으면 undefine, (null은 명시적으로 값없음)

    문자열 : 백틱과 '${}'사용

    참조형 object와 array : const로 선언해도 객체의 속성이나 배열의 요소는 변경할 수 있다

    형변환

    산술, 비교, 논리, 삼항 연산자 (===엄격한 비교)

    for while

    내부적으로 for문을 돌리는 함수 : .filter(함수)

        콜백 펑션(화살표 함수 형태로 많이 씀)

    함수 파라미터 아규먼트

        파리미터 배열 넘길떄 ...numbers

    클로저 : 함수가 자신이 선언된 스코프의 변수를 기억하는 성질

    function makeCounter() {
    let count = 0; return () => ++count; }
    const next = makeCounter();
    console.log(next(), next());

    배열 순회(forEach)

    배열 메서드(map, filter,find, some, reduce, sort)

    스프레드 연산

    DOM(문서 객체 모델): 웹 문서를 구조화하여 프로그래밍언어에서 접근-조작할 수 있게 한 표준 인터페이스(트리구조)(문서의 각요소를 객체로)(자바 스크립트로 실시간 변경)

        document.getElementById(요소)
        document.querySelector(선택자)

        조작할 요소를 파일 맨 위에서 한번만 선택해 상수에 담아둠

    html의 'data-*' 속성은 자바스크립트에서 element.dataset.이름으로 읽고 쓴다, 요소에 직접 데이터를 붙여 두는 표준 방법


    호이스팅 : 선언만 만저 올라감, 할당은 나중

    태그를 구조화된 데이터로 바꿈(객체) ,트리구조 객체로 구조화 DOM

    버블링 캡처링


    자바스크립트는 아이디에 태그가 붙었을때 찾기쉬움

    동기 방식, 비동기 방식, 자바스크립츠는 비동기

        promise, async(항상 promise반환) await

    localstorager: 같은서버, 같은 포트면 각 페이지끼리 데이터 공유 가능, 문자열만 저장, 배열 저장할려면 json(객체를 문자열로 바꿔줌) -> stringify 와 parse 차이
