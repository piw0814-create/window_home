## 1주

7/15

    제너레이터로 대용량 파일을 메모리 아끼며 처리하기

    dataclass로 데이터 레코드 깔끔하게 정의하기

    Pydantic으로 입력값 검증하기

    functools·파일 IO·pathlib·예외를 한 흐름으로

    pytest 로 정제 함수 테스트하고 Ruff 로 정리

    asyncio·httpx 로 여러 API를 동시에 수집

    Parquet으로 저장하고 필요한 열만 골라 읽기

    수집→검증→저장을 한 스크립트로 — 미니 데이터 파이프라인

    예상되는 문제점들 예외 처리 신경쓰기

    practice1: 매출 레코드 집계 — Counter·defaultdict·컴프리헨션

    TypedDict + mypy — dict 에 타입 계약을 새겨 실행 전에 오류 잡기

    ProcessPoolExecutor — CPU 무거운 전처리를 코어 수만큼 병렬로

    cProfile — 느린 함수를 느낌이 아니라 측정으로 찾기

    practice2 : safe_load_csv와 Pydantic SalesRecord 스키마, valid/errors 분리 저장과 재로딩 검증

    종합실습 1 지정 API — countries.dev 국가 정보 조회 (보완 안내)

        공공 데이터포털 등 무료 JSON API에서 asyncio·httpx로 데이터를 비동기 수집하고, Pydantic으로 스키마를 검증한 뒤 Parquet로 저장하는 스크립트를 작성해 제출한다.

        위 스크립트의 수집·변환 함수에 pytest 테스트를 최소 2개 붙이고, ruff로 정리한 뒤 requirements.txt와 함께 Git으로 커밋해 재현 가능한 형태로 만든다.

7/16

    DataFrame 만들고 열 선택하기, 조건으로 행 필터링하기, groupby로 그룹별 평균 내기

    Polars Lazy API

    Plotly Express 로 인터랙티브 차트 한 줄

    t-검정으로 두 그룹 차이가 우연인지 확인

    sklearn Pipeline 으로 전처리+모델을 한 덩어리로 저장

    업종별 매출 합계 상위 뽑고 막대그래프를 파일로 저장하기

    ColumnTransformer — 숫자 열과 글자 열을 각각 다르게 전처리해 결합

    실습3: IQR 이상치 제거 후 named aggregation 집계 (Practice 3)

    실습3: DuckDB로 DataFrame에 바로 SQL 집계 (Practice 3)

    실습4: 카이제곱 검정 — 두 범주형 변수의 독립성 (Practice 4)

    pivot_table 과 merge — 엑셀 피벗과 SQL JOIN 을 Pandas 로

    Pandas 2.x Copy-on-Write — 걸러낸 조각을 고칠 때의 함정

    apply 대신 벡터화 — str·dt 접근자로 열 전체를 한 번에

    schedule 로 매일 아침 리포트 자동 실행 — 반복 분석 자동화

7/20

    DBeaver 이용법.

    표를 키로 연결해 정합성 보장.

    SQL 질의 -> 파서 -> 옵티마이저 -> 실행기 -> 저장 엔진

    NULL값 잘 대응하기

    ERD

    PK, 후보키, 대리키, 왜래식별자, 자연키

    키와 무결성 제약조건

    정규화 = 하나의 사실을 한 곳에만 저장하도록 함수 종속 기준으로 표를 나누는 작업 : 1NF, 2NF, 3NF

    반정규화 - 너무 정규화 하면 속도 느려질수 있음, 병목이 확인된 곳만 선택적으로 반정규화

    좋은 데이터 모델 체크리스트

        모든 엔티티에 PK가 있다, 속성이 원자값, 하나의 사실은 한곳(원본1개->나머지 참조), 관계,선택성 명확, 명명규칙 명확, FK로 참조 무결성 보장

        계산 가능한건 컴럼으로 만들지 않기

7/21

    join 방식들


    옵티마이저 : nested loop join , hash join, sort merge join


    상관 서브 쿼리의 비용과 완화

    바깥 N행 -> 서브쿼리 최대 N회 실행, 바깥이 클수록 비용이 선형증가

    완화
    1 표준 형태로 작성해 Unnesting 유도
    2 서브쿼리 조인 컬럼에 인덱스 제공
    3 집계 후 조인(인라인 뷰·CTE)으로 재작성
    4 결측 처리는 NOT EXISTS

    피하기
    select 절의 무거운 상관 스칼라 반복
    Not in + null 가능 목록
    불필요하게 깊은 중첩
    측정 없는 추측성 재작성

    CTE

    집계 함수 : count(*), count(col), count(distinct), sum-avg, max-min : null 취급 잘 알기

    윈도우 함수

    다중 그룹핑 -> 주의: 이 경우는 실행순서에서 별칭 사용이 가능?, select의 비집계 컬럼은 모두 group by에 있어야함

    집계 함수 문제 -> 윈도우 함수

    트래잭션 : 원자성 일관성 고립성 지속성

        동시성 제어 : MVCC, 락, 데드락, 격리

7/22

    B+tree 구조

    삽입과 노드 분할

    인덱스 트레이드 오프 : 읽기는 빨라지지만 데이터 변경 시 인덱스도 갱신 -> 쓰기느려짐 + 공간추가

    클러스터형 인덱스, 비클러스터형

    복합 인덱스 : 순서가 중요 (=조건을 앞, 범위 조건 뒤)(=을 뒤에달면 풀스캔 위험!)

    covering index

    선택도와 인덱스 효율

    explain-> analyze -> 트리 읽기(노드 안->밖), 병목 찾기(가장 큰 노드)

    buffers 진짜 i/o비용

    스캔 방식 선택 기준 : 인덱스 온리, 인덱스스캔, bitmap scan, seq scan ->>옵티마이저가 정함

    조인 방식 : nested loop, hash join, merge join
    참고 : 디스크 스필

    병목 진단 : 풀스캔?, 예상vs실제 row검사, 정렬-해시가 디스크로 넘침?(work메모리 부족->조정-인덱스정렬), 많이 읽고 나중에 버림?(필터를 일찍->조건-조인 순서), 조인 방식이 적절한가(행추정 오류 -> 통계,인덱스)

    쿼리 리라이팅 : SARgable 조건, 필요한 컬럼,행만(명시잘하기)(일찎필터), 적절한 스캔 유도, 불필요 연산 제거(습관적 distinct, order by, 영향없는 서브쿼리,   union(중복이 없는데 사용)), 집합적 사고

        참고 : 조건을 갈래로 나눠 UNION 으로 합치면 갈래마다 자신의 인덱스(a용·b용)를 탈 수 있다.

        느린 상관서브쿼리(조인,집계 후 처리), not in (null 위험), 깊은 중첩 서브쿼리(CTE단계화), 스칼라 서브쿼리 반복(조인으로 한번에)

        핵심 코드에 반복문(for/while)으로 쿼리를 반복 호출하는 게 보이면 “집합으로 바꿀 수 있나 확인

        페이지네이션 : 오프셋 vs 키셋

        집계 튜닝 - 미리 계산하기 (materialized view로 캐시)

        정렬 튜닝 - 인덱스로 정렬 생략(drder by 컬럼에 인덱스->정렬 생략), 상위 n, 복합 정렬은 인덱스 순서-방향일치, work_men 초과 시 디스크 정렬

        N+1 제거 : 왕복 줄이기 -> 한번의 조인으로 일괄 조회, in으로 묶어 조회(바인딩도), orm은 eager loading/명시적 조인

        파티셔닝 : 큰 테이블을 쪼개기 (인덱스로 부족한 초대용량,시계열의 마지막 카드)

7/23

    요청/응답, 렌더링, 역할 분담, 개발환경

    HTML

    파싱, 렌더 트리, 레이 아웃, 페인트

    HTML(구조) -> CSS(표현) -> JavaScript(동작)

    DTD(문서형 정의) -> 문서의 시작과 끝 -> 문서 정보(메타데이터) -> (브라우저에 표시할 내용)

    태그, 요소(태그 + 내용), 속성(여는 태그 안에 이름=값 형태로 추가 정보 지정)(id, class, style lang, hidden)

    시맨틱 태그 : 문서의 구조와 의미를 명확히 나타내는 HTML5 태그: 가독성(코드 구조가 명확), 접근성(화면 낭독기가 콘탠츠가 콘텐츠 구조를 더 잘 이해한다), SEO(검색 엔진이 주요 정보를 효율적으로 파악)

    css

7/24

    호이스팅 : 선언만 만저 올라감, 할당은 나중

    태그를 구조화된 데이터로 바꿈(객체) ,트리구조 객체로 구조화 DOM

7/27

    내가 데이터(자원)를 어떻게 보고있는지(관점)(어떤 도메인에선 어떤 데이터가 중요한지 파악), 따라서 어떤 기법을 쓸껀지 결정(통계,ml,dl)

    데이터를 보고, 타겟이 되는 분석 범위 안에서 어떤 변수들이 해당 타겟의 특징을 잘 구분될수 있는지 파악.

7/28

    주의 사항 : 회귀분석의 가정, 다중공선성, 이상치의 영향, 변수 선택

7/31

    입력 데이터, 제공 결과, 사용 위치, 비용, 보안, 장애 대응까지 포함한 전체 흐름을 먼저 설계해야 한다. 각 구성요소의 책임을 나눈 뒤 적절한 기술을 배치한다.

    순수 Java로 HTTP 라우팅, 요청 파싱, 객체 생성과 연결, JDBC DB 처리, BCrypt 비밀번호 해싱, 세션·쿠키, JWT 발급과 검증을 직접 구현. 이를 통해 객체와 메서드의 호출 관계를 이해하고, Spring Boot가 반복적인 서버 설정과 객체 관리를 자동화하는 이유를 체감

8/3

    @붙음
    ↓
    Spring이 읽음
    ↓
    해당 클래스/메서드의 역할을 파악
    ↓
    필요한 객체 생성·연결·HTTP 처리 등을 자동 수행


    solid원칙(결국 역할과 책임을 구분하기 위함)

    S : 단일 책임 원칙 : 하나의 클래스는 하나의 책임만 가능 : controller, servic, repository등 계층 분리
    O : 개방-폐쇄 원칙 : 기존 코드는 수정하지 않고 기능을 확장할 수 있어야한다 : 인터페이스 기반 구현체 추가, 전략 패턴 활용
    L : 리스코프 치환 원칙 : 부모 타입 대신 자식 객체를 사용해도 동일하게 동작해야한다 : 인터페이스 구현체 교체 가능
    I : 인터페이스 분리 원칙 : 필요한 기능만 가진 작은 인터페이스로 분리한다. : 역할별 인터페이스 설계(작고 구체적인 인터페이스 여러개)
    D : 의존성 역전 원칙 : 구현 클래스가 아닌 추상화(인터페이스)에 의존한다 : soring의 DI 와 IoC의 핵심 원칙(객체 연결을 인터페이스로)

8/4

    Java가 담당하는 건 이런 부분들

    누가 이 AI를 사용할 수 있는가 → Security / Authentication
    어떤 데이터를 AI에 보낼 것인가 → DTO / Service
    업무 데이터를 어디서 가져오는가 → Repository / DB
    언제 AI를 호출할 것인가 → Service
    AI가 실패하면 어떻게 할 것인가 → Exception
    AI 결과를 누구에게 어떤 형태로 보여줄 것인가 → Controller / REST API
    기존 업무 기능과 AI 기능을 어떻게 연결할 것인가 → Architecture / DI

    우리는 현재 하고있는 업무에 ai기능을 추가해야된다. 현재 대부분은 java로 구성되어 있기 때문에 java를 알아둬야한다
    ai로 업무관계자들한테 가치를 제공해야되는데 업무에 데이터를 가지고 하는 것에 있어서 중심이 자바가된다
    개발자로 바라보지말고, ai 서비스의 백엔드로 바라봐야됨.(그래서 주로 아키텍쳐 얘기를 하는것)
    그래서 나중에 이러한 요소와 기술이 나중에 어떤식으로 사용할 것인지를 고민해라

    디렉토리 구조 -> 확장 수정 용이
    클래스 변수 선언(다른 클래스와 연결함)
    나눠놨으니까 서로 쓸때 연결(@어노테이션 사용)
    스프링부트가 @있는거 자동실행 되고 시작

8/5

    Spring Security 필터 체인- HTTP 요청 처리 순서
    HTTP 요청 → SecurityFilter Chain → JWT Filter → Authentication Manager → Controller

8/6

    에자일 방법론
    불확실성이 높고 변경이 많을수록 → 애자일에 유리
    요구사항·설계가 명확하고 변경 비용이 클수록 → 계획 중심 방식에 유리
    건물 설계, 금융 핵심 시스템, 하드웨어 연동처럼 중간에 설계를 바꾸는 비용이 매우 큰 프로젝트라면 처음부터 치밀하게 설계하는 게 중요

    반대로 애자일은, 처음부터 정답을 모르는 경우(데이터가 분리가 잘되는 상황)
    대략적인 방향 → 작은 기능 구현 → 사용자 확인 → 수정 → 다음 기능 → 다시 확인

    이런 상황에 강함

    애자일 개요(필요성,비교,적합상황 + 도입프로세스,실패패턴,전환 체크리스트), 스크럼(구조 산추룰+이벤트 실전가이드), user story 백로그(정의 원칙 그루밍 프로세스 before/after backlog 채우기)


    사용자 이야기(User Story)

    - 사용자 관점에서 짧게 기술된 기능에 대한 설명
    - 제품에 담고자 하는 기능 및 요구사항이라 볼 수도 있음
    - 제품과 관련하여 해야 할 일을 의미하는 것은 무엇이나 제품 백로그에 포함됨(제품 특징, 기능, 기술, 개선사항, 오류 등)

    제품 백로그(Product Backlog)

    - 우선순위가 부여된 User Story의 집합체 목록
    - 제품 백로그의 Ownership은 제품 책임자가 가짐

    요구사항 ≒ 사용자스토리 ≒ 백로그 ≒ 일감
    팀이 일하는 근거는 백로그가 되며, 모든 커뮤니케이션 및 성과, 측정은 백로그를 중심으로 이루어짐

8/7

    AI 서비스는 모델 성능·사용자 반응·데이터에 따라 결과와 요구사항이 계속 바뀌고, 새로운 모델이나 에이전트 도구가 끊임없이 실험되고 교체. 이런 불확실성과 잦은 변화가 AI 소프트웨어 개발의 본질적인 특성이기 때문에, 작게 나눠 자주 검증하는 Agile과 모듈 단위로 독립적으로 교체 가능한 MSA가 유난히 잘 맞는 조합

    기획
    이해관계자 가치(Pain Point)
    이를 해결하기 위한 AI 솔루션
    아키텍처 구성도
    API 명세
    동작 화면 스냅샷

8/10, 8/11, 8/12

- vue는 MVVM 아키텍쳐 패턴을 따른다.(model(데이터), 뷰엔진이 뷰와 데이터를 연결 시켜줌)
- virtual DOM(가상 DOM을 비교해서 바뀐 실제 DOM만 수정)
- 데이터 바인딩 : v-model directive
- componet based architecture (Props, emit)
- CSR : 브라우저가 자바스크립트를 실행하여 직접 화면을 그리는 방식(클라이언트가 직접), 서버는 json 데이터만 제공하는 형태
- SPA(index.html 하나를 기반으로 Vue Router와 컴포넌트를 이용해 페이지 전체를 새로고침하지 않고 필요한 화면 부분만 교체하는 구조, 초기 로딩만 오래걸리고, 후엔 필요 json데이터만 받아서 빠름)
- REST API
- Axios

8/13

- 배포과정 정리
  - ESLint와 Oxlint로 결함 가능성이 있는 코드를 검사한다.
  - Prettier로 프로젝트 전체의 코드 형식을 통일한다.
  - .env로 환경별 설정값을 관리한다.
  - 공개 변수와 서버 비밀키를 구분한다.
  - Vite가 소스 코드를 프로덕션용으로 번들링한다.
  - 완성된 결과물은 dist 폴더에 생성된다.
  - Vercel이 정적 파일과 서버 함수를 함께 배포한다.
  - GitHub를 연결하면 Push 이후 배포를 자동화한다.

8/14, 8/15

- 취업특강

8/18, 8/19, 8/20

- 미니 프로젝트
- 프로젝트 착수 및 기획
  - 목적 정의 : 프로젝트의 배경, 해결하려는 문제, 비즈니스 목표를 명확히 정의
  - 자원 산정
  - WBS
- 요구사항 분석 및 설계 : 화면 및 시스템 구조 설계
  - 요구사항 정의서 작성 : 고객 및 사용자가 요구하는 기능과 비기능(보안,성능 등) 요소를 정의
  - UI/UX 설계, 와이어프레임 및 스토리 보드(기획서)를 제작하여 사용자 화면 흐름을 설계
  - 아키텍쳐 및 DB설계 : 시스템 데이터 베이스(ERD)구조, API 규격, 서버 인프라 환경 설계
- 개발 및 구현 : 프론트엔드 및 백엔드 코딩
  - 프론트 엔드
  - 백엔드 개발 : 로직, API서버, DB 연동 작업
  - 형상 관리 : 도구를 활용해 코드 병합 및 관리를 수행
- 테스트 및 QA
  - 단위 통합 테스트
  - QA 시나리오 기반 버그를 발굴하고 수정
  - 사용자 수용 테스트
- 배포 및 유지보수
  - 상용배포
  - 안정화 및 모니터링
  - 유지보수

8/21

- 도메인 특강(반도체)

8/24

- 도메인 역량, 엔지니어 역량이 중요(요구사항 정리, 기술은 시대에 따라 계속 바뀜, 도구인 ai 어떻게 잘 사용할까), 도메인 지식과 엔지니어링 역량을 기반으로 고객들의 패인포인트를 파악해서 문제를 해결해주는 업무
- 데이터가 무척이나 중요함! 아직 데이터가 있으나 준비가 안되어있음. 기업이 얼마나 최신 데이터를 제공할수 있나, 데이터 정제를 하는게 아직 제대로 안되있음.
- 좋은 프롬프트 3대 조건
  - 명확성, 목적 대상 범위를 분명히
  - 구체성 분량 형식 조건을 구체적으로
  - 맥락, 구체적 자료
- 5가지 요소 : 역할, 목적, 맥락, 제약, 출력형식
- 한계 : 최신성, 사내비공개 지식, 긴맥락, 외부 도구, 실시간
  - 필요한 맥락 쥐어주기, 도구 쓰게하기

8/25, 8,26

- 토크나제이션
- 토큰임베딩
- 어텐션
- Attention은 Q, K, V를 이용하여 토큰 간 관계를 계산하고 필요한 문맥 정보를 결합하는 역할을 하고,FeedForward는 Attention을 통해 문맥이 반영된 각 토큰의 feature를 비선형적으로 변환하고 가공하는 역할을 한다. 여기에 LayerNorm은 각 단계의 값의 범위를 안정화하고, Residual Connection은 기존 정보를 보존하면서 새로운 정보를 추가하고 gradient가 깊은 네트워크를 통과하기 쉽게 만든다. LLM은 단순히 “다음 단어를 맞히는 모델”이 아니라, 각 토큰을 임베딩한 뒤 Attention으로 문맥을 구성하고 FeedForward로 그 표현을 가공하는 과정을 여러 층 반복하여 다음 토큰의 확률을 계산하는 구조

8/27, 8/28

- 회사에서 쓸때 도메인 지식을 활용한 rag과 fintunning 을 언제 어디서 무슨 데이터에 쓸지 결정하는게 중요하다.
- 변경이 잦거나 근거 문서가 중요한 사내 지식은 RAG로 연결하고, 모델의 응답 방식이나 특정 업무 패턴 자체를 학습시켜야 하는 경우에는 Fine-tuning을 고려하는 등, 목적과 데이터 특성에 따라 적절한 방법을 선택하는 것이 중요하다고 생각

- sLLM은 크기가 작아 특정 도메인 특화 및 경량 배포 가능, PEFT(LoRA/QLoRA)를 통해 빠르고 경제적인 파인튜닝 가능
  - 막대한 인프라 및 운영비용(TCO)절감
  - 데이터 보안 및 개인정보 보호(온프레미스 구축 용이, 규제 준수)
  - 실시간 추론 속도 및 낮은 지연시간

- 양자화,PEFT, LoRA,QLoRA, 파인튜닝, Rag결합, 고성능 추론 및 서빙 프레임워크 도입, 지속적인 평가와 모니터링

- Fine-tuning에 잘 맞음
  - 우리 회사 답변 형식
  - 특정 문체
  - 분류 기준
  - 업무 처리 패턴
  - 특정 작업 수행 방식

- RAG에 잘 맞음
  - 최신 정책
  - 사내 매뉴얼
  - 제품 스펙
  - 가격
  - 버전
  - 자주 바뀌는 사실

8/31

- 머신러닝 vs 딥러닝
  - 머신러닝은 정형데이터에 강점(참고. 추론통계를 대부분 대체중)
  - 테스트, 이미지, 음성...등에 딥러닝 강점(피처 자동 추출)
- 특정 전문 분석은 ML/DL에 맡기고, 사용자와의 소통 및 결과 요약은 생성형 ai가 담당
- 주어진 과제에 따라서 다양한 분석 기법을 설정하고, 어떤 모델을 쓰고 설계할지 결정 및 설명할 수 있어야 함
- 슈퍼바이즈드 러닝(범주형: 클래시피케이션, 연속형:레그레이션), 언슈퍼바이즈드 러닝(클러스터링 : k-means, 연관석 분석, 차원축소)
- 앙상블 : 베이스 앙상블, 보팅, 부스팅, 메타 앙상블
- 디시전 트리, 랜덤포레스트, xg부스트, lightGBM, SVM
- 회귀분석
  - 다중 공선성 문제(회귀계수 불안정), 과적합
  - regulatization은 회귀 계수 크기를 제한(패널티)하여 모델의 복잡도를 줄이고, 과적합을 방지하여 일반화 성능을 향상
  - LASSO, RIDGE

9/1

- ai시대에 중요한 것은 데이터!, 실제 사용자의 관점 활용영역도 필요함 -> 데이터분석에 활용
- 피쳐엔지니어링(이격도, lag, NVI, PVI, 다중공선성제거, 파생변수 설정, 비율타겟정의..) -> 모델분석(다들 비슷)
- 결국 데이터의 이해와 정제가 중요함
- llm만 이용하는것의 문제 : 재현성, 보안, 대용량 데이터 처리 한계, 모델 커스터마이징 불가
  - Code Generation Only, 실제 모델 학습과 운영은 전통적인 ML파이프라인으로 수행하는 경우가 많음
- **Association Rule**, PCA, **Anomaly Detection**, **Time series forecation**,
- 알고리즘 선택
  - 업무 자동화 : 동일 성능이면 단순모델, 노이즈 견고, 다양한 데이터 타입 처리, Linear/ridge/lasso, randon forest ...
  - 모델 설명력 : 성능보다 왜 그런 결과가 나왔나, feature 영향도 파악, 의사결정과정의 투명성 보장, linear, logistic, Regression, decision tree
  - 모델 정확도 : 비선형적이고 복잡한 패턴 학습, 대규모 데이터셋, 여러 모델 조합으로 오차 최소화, boosting, ensemble, 딥러닝..
  - 구조 이해/탐색/ 이상감지 : 데이터의 내재된 구조와 분포 특성 파악, 잠재적 리스크 파악, EDA, k-means, PCA, isolation forest
- 딥러닝
  - 손실함수
  - 역전파
  - 옵티마이저(학습률 조절) : SGD, Momentum, RMSprop, Adam
    - 경사하강법
    - 학습률
  - 스케줄러 : 학습 진행 상황(에포크, 벨리데이션 로스)에 따라 전체 학습률을 동적으로 조절

9/2

- 그레디언트 베니싱
- CNN합성곱 연산, 풀링, 플랫튼
- RNN 순환 구조, 은닉상태, 셀, seq2seq, 보틀넥 현상, 순차 처리 문제
- 트랜스포머

9/3

- 비즈니스 이해, 타겟 정의, 피쳐도출이 데이터 분석의 핵심!
- 데이터 분석 프로세스(자기만의 프로세스 정립하기)
  - 비즈니스 이해, 데이터 준비, 데이터 전처리, 모델링, 모델 평가, 모델 적용
  - 분석주제, 타겟 정의, 피쳐도출, 분석요건 정의(모집단설정, 기간, 분석기법), 분석 모델링....
- 데이터 특성 이해
  - 정형,(피쳐 엔지니어링+ML) 비정형(파인튜닝,DL), 사이즈((작으면-복잡도 낮은모델, 비지도학습, 룰베이스), (크면-모델보다 학습/추론 인프라, 자동화 설계 중계)), 배치(일정주기로 데이터 대량 처리-복잡한 모델과 피처 사용 가능), real-time(모델 정확도 보다 응답시간, 운영안정성이 더 중요).
- 데이터 준비(가장 시간 많이 걸림)
  - 데이터 구축 프로세스
    - 데이터 정제, 변환, 통합
  - 데이터 품질 점검이 진짜 중요!(데이터를 봤을때 어떤 측면으로, 어떤 관점으로 바라볼지에 따라 분석가의 역량이 차이남)
- 피쳐 엔지니어링
  - 피쳐 추출
    - 피쳐 트랜스폼 : 기계적,수학적 추출, 사람의 해석 없이도 생성 가능한 피쳐
    - 파생변수생성!! : 도메인 지식 기반 추출, 창의력
  - 도메인 지식, 경험 및 사례로 후보 피쳐 생성 -> 데이터 기초 탐색으로 데이터 신뢰성 검증, 통계/ML 분석으로 피쳐 중요도 검증, 업무 논리적 판단에 의한 검증(Logical trend 분석), 최종 피쳐 선정
  - 논리성 검증 + 모델 변별력 + 모델 안정성
    - Logical trend분석은 변수의 영향 방향과 크기가 비즈니스 측면에서 논리적으로 맞는지를 검증
    - 변별력이 있나
    - 시간 등에 따라 피쳐가 얼마나 안정적이냐도 봄
- 데이터 분할
- 모델 최적화
- 모델 평가 지표, 비교할 베이스라인or나이브 모델 필요
  - class : 혼동행렬
    - accuracy
    - recall
    - precision
    - F1
    - 프리시전과 리콜은 트레이드오프의 관계
  - probability
    - ROC,AUC
    - LIFT
    - AUC로 전체 집단의 모형 성능을 평가하고, LIFT로 관심 집단의 성능을 평가
  - 일반적으로 많이 : Accuracy, F1, AUC
  - 특정 목적 : Precision, Recall, LIFT
  - 회귀 : MAE, MSE, RMSE, MAPE, R-Squre
- 모델 해석(데이터 리키지 주의)
- 모델 모니터링
  - Model Drift
    - 데이터 드리프트(데이터 변화)
      - 피쳐 변경 없이 가중치만 조정 Re-training - 피쳐 계수 변경
    - 프레딕션 드리프트(모델출력의 변화)
      - 의사결정 위한 예측 확률 기준값 조정, Threshold Adjustment - threshold 조정
    - concept drift(데이터 관계의 변화)
      - EDA부터 피쳐 생성 등 모델 재구축, Re-modeling

9/4

- 데이터 분석은 프로세스다.
- 입수 데이터는 충분히 의심하라
- 모델 개발 보다 모델 설계가 더 중요
  - 분석 모집단 구성, segment/target/feature 정의, 분석기법/검증 및 평가 방법 설계 등
- 모델 학습보다 모델 검증 및 평가가 더 중요하다
- 모델 성능은 피쳐 성능에 달려있다
- 높은 성능의 모델 보다 중요한 것은 잘쓰이는 모델이다.

- 좋은 모델링은 좋은 알고리즘을 찾는 문제가 아니라, 데이터를 제대로 이해하고 문제를 설계한 뒤 적절한 Feature와 평가 방법을 통해 실제로 활용 가능한 모델을 만드는 과정
